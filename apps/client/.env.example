# API Base URL
# Leave empty for production (uses same origin)
# Set to http://localhost:3005/api for development if not using proxy
VITE_API_URL=

# Supabase Configuration (if needed)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
